extends RayCast2D
var body

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	#if get_collider() and not get_collider() == null and get_collider().is_in_group("Player"):
	#	get_parent()._spot_player(get_collider())
	pass
